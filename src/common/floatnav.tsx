import React from 'react';

// ================================================

export default function FloatNav({ children }: any) {
	return (
		<React.Fragment>
			<div className="float-nav">{children}</div>
		</React.Fragment>
	);
}
